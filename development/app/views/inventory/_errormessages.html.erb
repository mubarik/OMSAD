<% unless error.blank?  %>
  <% if error.errors.any? %>
    <div class="error_messages">
      <h2 class ="errorMessagesheading">
        <%= pluralize(error.errors.count, "error") %> prohibited this record from being saved:
      </h2>
      <ul>
        <% for message in error.errors.full_messages %>
          <li>
            <%= message %>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>
<%end%>
